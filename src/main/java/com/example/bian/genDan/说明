

根据双向跟单-待做（同时持有做多和做空时，不支持，如果只是双向，不同时做是支持的）

找做波段的，时间越久越好

下单记录和持仓记录可以不用登录，不用token和cookie，根据这个跟单（下单记录是否及时更新需要确认）

//首先查询10个历史订单，必须有一个全部平仓的，否则可能会增加很多历史记录，虽然不影响跟单，但是看着不舒服。为什么呢？因为部分清仓的单子，它的更新时间是变动的，不是不变的，所以map里就判断没有这个历史，会循环查询200个

当老师停止带单后，自动清空-已做
当获取老师已有订单有问题时，监控程序会清空，这时候是有问题的-已做
根据持仓数量监控-已做
监控转移-已做
同一个报错，最多发送10条-已做


positions
    只有这个接口需要cookie，
    当返回是00000，count=0时，就是cookie过期了，有问题了，正确的返回值里有很多做过的单子，根据单子是否有positionAmount判断是否是正在做的单子
    正确的返回是，把所有做过的symbol都会查出来，根据个数不等于0判断是否正在持仓

position-history
    持仓历史，看看是否可以根据这个监控，就不怕cookie过期了

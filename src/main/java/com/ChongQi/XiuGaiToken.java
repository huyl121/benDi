package com.ChongQi;

import com.alibaba.fastjson.JSONObject;
import com.example.bian.client.bushu.PrivateConfig;

import java.io.FileWriter;
import java.io.IOException;

import static com.example.bian.client.bushu.PrivateConfig.computer;

public class XiuGaiToken {

    public void method(String newToken) {
        /*String path = "/root/huyl/bushu/info.json";
        JSONObject config = PrivateConfig.readJsonFile(path);
        config.getJSONObject("genDan").put("token", newToken);
        try (FileWriter writer = new FileWriter(path)) {
            writer.write(config.toJSONString());
            System.out.println(computer + "：token替换成功");
        } catch (IOException e) {
            e.printStackTrace();
            System.out.println(computer + "：token替换失败");
        }*/






    }

}
